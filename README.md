# Render Chat

Render Chat is a Socket.IO-based chat server hosted on Render's free tier.

Clients are assigned unique names and colours for identification. These identities are kept in memory for each client IP address and disappear when the server closes. Render closes the server after a period of inactivity.

Let's see how long Render will host this thing for...
